# Python dependencies required ONLY for running the report generation scripts
# (generate_report.py and get_pages.py)
# Note: The main Multiplex Movie Booking System is a pure Java application and requires no external dependencies.

python-docx==1.1.0
Pillow==10.2.0
matplotlib==3.8.3
pywin32==306
